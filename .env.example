# Adnexum Inspector - Variables de entorno de ejemplo

# Backend (copiar a .env)
# Puerto del servidor (Railway lo setea automáticamente)
PORT=8000

# Frontend (configurar en Vercel o copiar a frontend/.env.local)
# URL de la API backend (cambiar al dominio de producción)
NEXT_PUBLIC_API_URL=http://localhost:8000
